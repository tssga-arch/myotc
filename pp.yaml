#
# Try pre-processor features
#
#define ABC 123

x{ABC}: 433

balh:
 #include demo2.yaml
cheh:
 - content: |
     #include --raw vars.bat
 - yaho: {{}}

base64: |
  #include --bin vars.bat
  #exec ls -l
     #include demo1/more_demo.yaml

oh: {ABC}
home: {HOME}
shell: {SHELL}
